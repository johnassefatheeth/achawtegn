import 'package:flutter/material.dart';

class vidlist extends StatelessWidget {
  const vidlist({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}